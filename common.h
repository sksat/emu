#ifndef COMMON_H_
#define COMMON_H_

#include <stdio.h>
#include <stdint.h>

//#define NO_DEBUG

#include "debug.h"

const size_t KB = 1024;
const size_t MB = (1024 * KB);
const size_t GB = (1024 * MB);

#endif
